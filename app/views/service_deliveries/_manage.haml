- if can? :manage, ServiceDelivery
  = link_to edit_service_delivery_path(object, parent_id: parent_id), class: 'btn btn-xs btn-success btn-outline' do
    = fa_icon('pencil')
  = remove_link(object, { case: 0 })
