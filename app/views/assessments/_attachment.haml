%div{class: 'modal fade', id: "attachments#{ad.id}", :role => "dialog"}
  .modal-dialog
    .modal-content
      .modal-header
        %button.close{"data-dismiss" => "modal", :type => "button"} ×
        %h4.modal-title= t('.attachments')
      .modal-body
        %table.table.table-bordered
          - ad.attachments.each_with_index do |attachment, index|
            %tr
              %td= original_filename(attachment)
              %td
                = link_to preview_or_download(attachment), attachment.url, class: 'btn btn-info btn-sm btn-download', target: target_blank(attachment)
              %td.vert-align.text-center
                = link_to client_assessment_path(@assessment.client, @assessment, assessment_domain_id: ad.id, file_index: index), method: :delete, data: { confirm: t('.are_you_sure') }, class: 'delete btn btn-outline btn-danger' do
                  = fa_icon('trash')
      .modal-footer
        %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} Close
