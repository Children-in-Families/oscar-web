.row
  .col-xs-12
    = simple_form_for @setting do |f|
      .ibox
        .ibox-title
          %h5.text-center= t('.title')
        .ibox-content
          .i-checks
            %label= t('settings.test_client')
            = f.input :test_client, as: :radio_buttons, collection: [['Yes', true],['No', false]], label: false
        .ibox-footer
          = f.submit t('save'), name: 'research_module' ,class: 'btn btn-primary', data: { disable_with: t('saving') }

