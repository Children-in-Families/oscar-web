- if controller_name != 'sessions'
  = link_to t('.login'), new_session_path(resource_name)
  %br/
- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
  = link_to t('.forget_password'), new_password_path(resource_name)
  %br
  = link_to t('.back'), root_url(host: request.domain, subdomain: 'start')
  %br/
- if devise_mapping.confirmable? && controller_name != 'confirmations'
  = link_to t('.receive_confirm_instruction'), new_confirmation_path(resource_name)
  %br/
- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
  = link_to t('.receive_unlock_instruction'), new_unlock_path(resource_name)
  %br/
- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to "Sign in with #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider)
    %br/
