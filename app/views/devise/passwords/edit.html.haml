.row
  .col-xs-12.col-sm-6.col-sm-offset-3
    .ibox.normal-space
      = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
        .ibox-title
          %h5= t('.change_your_password')
        .ibox-content
          = error_notification(f)
          = f.input :reset_password_token, as: :hidden
          = f.full_error :reset_password_token
          .form-inputs
            = f.input :password, label: t('.new_password'), required: true, autofocus: true
            = f.input :password_confirmation, label: t('.confirm_your_new_password'), required: true
        .ibox-footer
          = f.submit t('.change_my_password'), class: 'btn btn-outline btn-primary'
