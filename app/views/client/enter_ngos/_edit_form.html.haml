.modal.fade{ role: 'dialog', tabindex: '-1', id: "enter-ngo-form-#{enter_ngo.id}" }
  .modal-dialog{ role: 'document'}
    .modal-content
      .modal-header
        %button.close{"aria-label" => "#{t('.close')}", "data-dismiss" => 'modal'}
          %span{"aria-hidden" => 'true'} &times;
        %h4.modal-title
          = t('.accept_ngo_form')

      .modal-body
        = simple_form_for [@client, enter_ngo] do |f|
          = f.error_notification
          .form-inputs
            .form-group.date
              %label.date.required.control-label
                %abbr{ title: 'required' } *
                = t('.accepted_date')
              .input-group
                = f.text_field :accepted_date, readonly: true, class: 'date form-control date-picker', type: 'text', name: 'enter_ngo[accepted_date]'
                %span.input-group-addon
                  %i.fa.fa-calendar-check-o

          / .form-inputs
          /   = f.input :status, readonly: true, input_html: { value: 'Accepted' }

          .form-inputs
            .form-group.date
              %label.date.required.control-label
                %abbr{ title: 'required' } *
                = t('.case_worker_or_staff')
              = f.association :users, collection: @users, include_blank: false, multiple: true, label: false

          .text-right
            %button.btn.btn-default{"data-dismiss" => 'modal'}= t('cancel')
            = f.submit 'Accept' , class: 'btn btn-success agree-btn', name: 'accept-client'
