#home-index
  .row
    = render 'tasks_and_assessments'
    = render 'client'
    - if can? :manage, Family
      = render 'family'
  .row
    = render 'third_party'
  .row
    = render 'client_case_statistic'