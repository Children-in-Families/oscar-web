%ul{"data-#{klass.parameterize}": ''}
  - if params[:data] == 'recent'
    - return if properties.blank?
    %li= properties
  - else
    - properties.select(&:present?).each do |property|
      %li= property
