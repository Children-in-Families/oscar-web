- if params[:family_id]
  %div{ class: "#{program_stream.enrollment_status_label(@programmable, 'Family')}"}
    = program_stream.enrollment_status_value(@programmable, 'Family')
- if params[:community_id]
  %div{ class: "#{program_stream.enrollment_status_label(@programmable, 'Community')}"}
    = program_stream.enrollment_status_value(@programmable, 'Community')
