:plain
  var appearence = $("#{escape_javascript(render partial: 'notifications/family_referral')}");
  $('#program_stream_list.modal #active-program-stream-client .panel-body').html(appearence);
  toastr.clear()
  $('#program_stream_list.modal').modal('show');
