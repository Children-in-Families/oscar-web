= simple_form_for(domain_group ||= DomainGroup.new) do |f|
  .modal.fade{id: "domain_groupModal-#{domain_group.id}", "aria-labelledby" => "domain_groupModal-#{domain_group.id}Label", :role => "dialog", :tabindex => "-1"}
    .modal-dialog{:role => "document"}
      .modal-content
        .modal-header
          %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
            %span{"aria-hidden" => "true"} ×
          %h4.modal-title{ id: "domain_groupModal-#{domain_group.id}Label" }= t('.domain_group')
        .modal-body
          = error_notification(f)
          .form-inputs
            = f.input :name, label: t('.name')
            = f.input :description, input_html: { rows: '10' }, label: t('.description')
        .modal-footer
          .pull-left
            = f.submit t('.save') , class: 'btn btn-primary form-btn', data: { disable_with: t('saving') }
            %button.btn.btn-outline.btn-default{"data-dismiss" => "modal", :type => "button"}= t('.cancel')
