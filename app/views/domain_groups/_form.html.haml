= simple_form_for(@domain_group) do |f|
  .panel-body
    = error_notification(f)

    .form-inputs
      = f.input :name, label: t('.name')
      = f.input :description, input_html: { rows: '4' }, label: t('.description')
  .panel-footer
    .form-actions
      = f.button :submit,t('.save'), class: 'btn btn-primary form-btn'
      = link_to t('.cancel'), domain_groups_path, class: 'btn btn-default form-btn'
