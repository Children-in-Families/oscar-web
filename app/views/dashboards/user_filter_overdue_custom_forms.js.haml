:plain
  var appearence = $("#{escape_javascript(render partial: 'dashboards/overdues/client_custom_form')}");
  $('#assessment_list.modal #overdue-assessments .panel-body').html(appearence);
  toastr.clear()
  $('#assessment_list.modal').modal('show');
  $('input.i-checks').iCheck('uncheck')
  $('input#checkbox-form').iCheck('check')
  $('#assessment_list #users-filter').removeClass('hide')
  $('#assessment_list #filtered_by_user').attr('action', '#{dashboards_user_filter_overdue_custom_forms_path}');

