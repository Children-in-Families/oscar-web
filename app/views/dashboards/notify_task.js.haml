:plain
  var appearence = $("#{escape_javascript(render partial: 'dashboards/overdues/tasks')}");
  $('#active_tasks_list .modal-body').html(appearence);
  toastr.clear()
  $('#active_tasks_list').modal('show');
