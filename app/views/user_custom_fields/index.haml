.row
  .col-xs-12
    .ibox
      .ibox-title
        %h5= "#{@custom_field.form_title} "
        .ibox-tools
          - if can? :manage, UserCustomField
            = next_entity_type_custom_field(@user_custom_fields, @user, @custom_field)
            = link_to user_path(@user) do
              .btn.btn-outline.btn-success.button.form-btn
                = t('.back')
      .ibox-content
        .row
          - @user_custom_field.each do |user_custom_field|
            .col-xs-12.col-md-6
              .ibox.border-left-right.border-bottom
                .ibox-title
                  %h5
                    = t('.created_date')
                  %span.label.label-info
                    = date_format user_custom_field.created_at
                  .ibox-tools
                    - if can? :manage, UserCustomField
                      %small
                        = link_to edit_user_user_custom_field_path(@user, user_custom_field.id) do
                          .btn.btn-outline.btn-success
                            = fa_icon('pencil')
                      %small
                        = link_to user_user_custom_field_path(@user, user_custom_field.id, custom_field_id: user_custom_field.custom_field_id), method: :delete, data: { confirm: t('.are_you_sure') } do
                          .btn.btn-outline.btn-danger
                            = fa_icon('trash')
                .ibox-content
                  %table.table.table-bordered
                    - user_custom_field.properties_objs.each do |key, value|
                      %tr
                        %td.align-justify
                          = key.titleize
                        %td.align-justify
                          = display_custom_properties(value)
      .ibox-footer.text-center
        = paginate @user_custom_field
