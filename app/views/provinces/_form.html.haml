= simple_form_for(province ||= Province.new) do |f|
  .modal.fade{id: "provinceModal-#{province.id}", "aria-labelledby" => "provinceModal-#{province.id}Label", :role => "dialog", :tabindex => "-1"}
    .modal-dialog{:role => "document"}
      .modal-content
        .modal-header
          %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
            %span{"aria-hidden" => "true"} ×
          %h4.modal-title{ id: "provinceModal-#{province.id}Label" }= t('.province')
        .modal-body
          = error_notification(f)
          .form-inputs
            = f.input :name, label: t('.name')
        .modal-footer
          .pull-left
            = f.submit t('.save') , class: 'btn btn-primary form-btn'
            %button.btn.btn-default{:type => "button"}= t('.cancel')
