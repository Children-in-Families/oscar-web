-# .nested-fields
-#   .row
-#     - @needs.each do |need|
-#       .field.col-xs-12.col-sm-4
-#         = f.input :rank, as: :select, collection: ['១','២','៣','៤','៥','៦','៧','៨'], label: need.name
-#         = f.input :need_id, label: false, input_html: { value: need.id }

.nested-fields
  .row
    .field.col-xs-12.col-sm-4
      -# = f.input :rank, as: :select, collection: ['១','២','៣','៤','៥','៦','៧','៨', '៩', '១០', '១១', '១២', '១៣', '១៤', '១៥', '១៦'], label: need.name
      = f.input :need_id, label: false
