= @topic.posts.first.content

This email was sent to you because #{@topic.user.name}
included you in the private topic "#{@topic.title}".
#{link_to 'Go here', topic_path(@topic, subdomain: Organization.current.short_name)}
to view the conversation:

To unsubscribe from these emails, update your preferences here:
#{edit_preferences_url(nil, subdomain: Organization.current.short_name)}
